{
  "name": "@cursor/mcp-host",
  "version": "0.1.0",
  "private": true,
  "description": "Shared MCP host for mcp.boxd.sh",
  "type": "module",
  "scripts": {
    "build": "npm run build --prefix ../radicle && tsc",
    "postinstall": "npm run build",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2"
  },
  "engines": {
    "node": ">=20"
  }
}
